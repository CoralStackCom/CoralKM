import { TextProps } from 'react-native'

export interface SectionTitleProps extends TextProps {
  /* Content to be displayed as the section title
   * @type string
   * @example theme
   */
  value: string
}
