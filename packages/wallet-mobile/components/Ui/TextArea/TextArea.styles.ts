import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  textarea: {
    minHeight: 64,
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 6,
    borderWidth: 1,
    borderColor: '#E5E7EB',
    fontSize: 16,
    color: '#000',
    backgroundColor: '#FFF',
  },
})
