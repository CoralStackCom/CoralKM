# Changesets

This folder holds [changesets](https://github.com/changesets/changesets) for
versioning the protocol and packages (Phase 0 · INF-8).

For any protocol/package change, run `yarn changeset` and follow the prompts to
record a changelog entry and version bump. Requires `@changesets/cli`:

```bash
yarn add -D @changesets/cli
```
