{
  "name": "coralkm",
  "version": "0.1.0",
  "description": "A user-friendly protocol for decentralized key management",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "test": "yarn workspaces foreach -A run test",
    "test:ui": "yarn workspaces foreach -A run test:ui",
    "test:run": "yarn workspaces foreach -A run test:run",
    "lint": "yarn workspaces foreach -A run lint",
    "typecheck": "yarn workspaces foreach -A run typecheck",
    "clean": "yarn workspaces foreach -A run clean",
    "dev": "yarn workspaces foreach -A -p run dev"
  },
  "resolutions": {
    "@types/node": "24.5.2",
    "vitest": "3.2.4",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "*",
    "@typescript-eslint/eslint-plugin": "^8.44.1",
    "@typescript-eslint/parser": "^8.44.1",
    "@vitest/ui": "^3.2.4",
    "eslint": "^9.36.0",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.3.0",
    "rimraf": "^6.0.0",
    "typescript": "*",
    "vitest": "*"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "yarn@4.9.1"
}
